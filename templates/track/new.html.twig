{% extends 'base.html.twig' %}

{% block title %}{{ 'Tracks'|trans }}{% endblock %}

{% block content %}
    <div class="h-100 row justify-content-center align-items-center">
        <div class="col-12 col-md-6 col-lg-4 col-xl-3 mx-auto">
            <a href="{{ path('track_new_link') }}" class="btn btn-primary border-dark border-4 btn-lg p-5">
                <i class="fa fa-link"></i>
                {{ 'PastALink'|trans }}
            </a>
        </div>
        <div class="col-12 col-md-6 col-lg-4 col-xl-3 mx-auto">
            <a href="{{ path('track_new_upload') }}" class="btn btn-primary border-dark border-4 btn-lg p-5">
                <i class="fa fa-upload"></i>
                {{ 'UploadAFile'|trans }}
            </a>
        </div>
    </div>
{% endblock %}
