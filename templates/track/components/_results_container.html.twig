<div class="col-12 col-md-10 offset-md-1">
    <div class="row px-3"  {{ stimulus_target('async_search', 'container') }}>
        {{ include('track/components/_list.html.twig', {tracks}) }}
    </div>
</div>
{{ include('components/_loader.html.twig') }}
<div class="p-3 text-center">
    <button class="btn btn-primary" {{ stimulus_action('async_search', 'loadMore') }}>
        {{ 'LoadMoreTracks'|trans }}
    </button>
</div>
