{% if value is defined %}
    <p>
        {% if label is defined and label is not null %}
            <span>{{ label }} :</span>
            {% if url is defined and url is not null %}
                <a href="{{ url }}" class="text-dark"><u>{{ value }}</u></a>
            {% else %}
                <u>{{ value }}</u>
            {% endif %}
        {% else %}
                <span class="fs-4">{{ value }}</span>
        {% endif %}
    </p>
{% endif %}
