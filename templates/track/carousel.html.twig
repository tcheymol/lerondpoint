{% extends 'base.html.twig' %}

{% block title %}{{ 'Track'|trans }}{% endblock %}

{% block content %}
    <div
        class="text-dark vw-100 vh-100 position-absolute top-0 start-0 z-1k1"
        {{ stimulus_controller('go_to_on_escape', {path: path('track_show', {id: track.id}, true)}) }}
    >
        {{ include('track/components/glider/_glider.html.twig', {track}) }}
    </div>
{% endblock %}
