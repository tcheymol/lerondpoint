{% extends 'base.html.twig' %}

{% block content %}
    <div class="col d-flex flex-column text-dark">
        <div class="row flex-grow-1 d-flex align-content-center p-3">
            <div class="col-12 col-sm-10 offset-sm-1 col-lg-8 offset-lg-2">
                <div class="bg-{% block cardColor %}white{% endblock %} border border-dark border-4 p-{% block cardPadding %}4{% endblock %}">
                    {% if block('title') != '' %}
                        {% block cardTitle %}
                            <h1 class="fs-1">{{ block('title') }}</h1>
                        {% endblock %}
                    {% else %}
                    {% endif %}
                    <div class="text-start">
                        {% block cardContent %}{% endblock %}
                    </div>
                </div>
            </div>

        </div>
        <div class="row p-2">
            {% block afterCard %}{% endblock %}
        </div>
    </div>
{% endblock %}
