<div {{ stimulus_controller('places', {formName: 'collective'}) }}>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <div id="autocomplete" class="autocomplete-container position-relative"></div>
        <div class="text-end">
            <button class="btn btn-primary">{{ 'Create'|trans }}</button>
        </div>
    {{ form_end(form) }}
</div>
