{% extends 'templates/card_page.html.twig' %}

{% block title %}{{ 'Collective'|trans }}{% endblock %}
{% block bodybg %}secondary{% endblock %}

{% block cardContent %}
    <hr/>
    {% if collective.iconPath %}
        <div class="row">
            <div class="col-12 col-sm-6 offset-sm-3 d-flex justify-content-center">
                <img style="width: 150px" class="me-2" src="{{ collective.iconPath }}"  alt="{{ 'Collective'|trans }}"/>
            </div>
        </div>
    {% endif %}

    <h2 class="text-center mt-4">{{ collective.name }}</h2>

    {{ collective.shortDescription }}
    <br/>
    {{ collective.followUs }}
    <br/>
    {{ collective.contactUs }}
    <br/>
    {{ collective.description }}

    {% for action in collective.actions %}
        <span class="badge bg-secondary">{{ action.name }}</span>
    {% endfor %}

    <div class="row mt-4">
        <div class="col-10 offset-1 col-sm-6 offset-sm-3">
            <div class="row">
                {% for action in collective.actions %}
                    <div class="col h-100">
                        <div class="card d-flex align-items-center">
                            <img style="width: 50px" class="me-2" src="{{ action.iconPublicPath }}"  alt="{{ action.name }}"/>
                            <div>{{ action.name }}</div>
                        </div>
                    </div>
                {% endfor %}
            </div>
        </div>
    </div>
{% endblock %}
