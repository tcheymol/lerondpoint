<div class="card d-flex align-items-center justify-content-center p-1 mx-2 mt-2 flex-grow-1">
    <img style="width: 32px" class="me-2" src="{{ action.iconPublicPath }}"  alt="{{ action.name }}"/>
    <div>{{ action.name }}</div>
</div>
