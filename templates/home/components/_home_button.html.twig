<div class="home-button col-6 text-start text-md-center z-1 {{ position }}">
    <a class="btn btn-{{ color}} rounded-5 cloudy" href="{{ url }}">
        <img class="mh-150px" src="{{ pictureUrl }}" alt="{{ label }}">
    </a>
</div>
