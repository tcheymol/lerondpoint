{% extends 'base.html.twig' %}

{% block title %}{{ 'HomeTitle'|trans }}{% endblock %}

{% block content %}
    <main class="px-3">
      <h1>{{ 'HomeTitle'|trans }}</h1>
      <p class="lead">{{ 'HomeSubtitle'|trans }}</p>
      <p class="lead">
        <a href="#" class="btn btn-lg btn-secondary fw-bold border-white bg-white">{{ 'KnowMore'|trans }}</a>
      </p>
    </main>
{% endblock %}
