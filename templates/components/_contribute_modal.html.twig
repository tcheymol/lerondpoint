<div class="contribute-modal bg-white rounded-3 p-3 text-dark d-flex flex-column justify-content-center align-items-center">
    <p><b>{{ 'ParticipateSubtitle'|trans }}</b></p>

    <div class="flex-grow-1 grotesk d-flex">
        <div class="d-flex flex-column flex-grow-1 justify-content-center">
            <a class="btn btn-success mt-3 btn-lg p-4" href="{{ path('track_new') }}">{{ 'CreateTrack'|trans }}</a>
            <a class="btn btn-primary mt-3 btn-lg p-4" href="{{ path('collective_new') }}">{{ 'CreateCollective'|trans }}</a>
            <a class="btn btn-secondary mt-3 btn-lg p-4" href="{{ path('app_login') }}">{{ 'Login'|trans }}</a>
        </div>
    </div>
    <p>
        <u>{{ 'KnowMore'|trans }}</u> :
        <b>{{ 'ParticipateKnowMore'|trans }}</b>
    </p>

</div>
