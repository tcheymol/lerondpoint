<div class="nav nav-tabs d-flex d-lg-none">
    <a
            class="flex-grow-1 text-dark text-center nav-link hover-underline rounded-0
                    {{ map == 'metropolis' ? 'active bg-light-subtle' : ''}}"
                    {{ map == 'metropolis' ? 'aria-current="page"' : ''}}

            href="{{ path('collective_index', { map: 'metropolis' }) }}"
    >{{ 'Metropolis'|trans }}</a>
    <a
            class="flex-grow-1 text-dark text-center nav-link hover-underline rounded-0
                    {{ map == 'droms' ? 'active bg-light-subtle' : ''}}"
                    {{ map == 'droms' ? 'aria-current="page"' : ''}}
            href="{{ path('collective_index', { map: 'droms' }) }}"
    >{{ 'Droms'|trans }}</a>
</div>